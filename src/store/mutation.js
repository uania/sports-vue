const mutations = {
    // 全局加载条
    load: (state, load) => {
        state.load = load
    },
    clientWidth: (state, clientWidth) => {
        state.clientWidth = clientWidth
    }
}

export default mutations